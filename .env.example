# ===== CONFIGURACIÓN FORO CON AUTO-DETECCIÓN =====
# Copiar este archivo como .env y personalizar según tu entorno

# 🧠 AUTO-DETECCIÓN INTELIGENTE DE FRONTEND_URL
# El sistema detecta automáticamente la mejor URL según tu configuración:
#
# Si configuras SERVER_IP:
#   - Dominio (ej: midominio.com) → https://midominio.com  
#   - IP (ej: 192.168.1.100) → http://192.168.1.100:9050
#
# Si no configuras nada:
#   - Detecta automáticamente tu IP local → http://TU_IP_LOCAL:9050

# 🌐 CONFIGURACIÓN DE RED (OPCIONAL - SE AUTO-DETECTA)
# Descomenta y configura solo si quieres forzar valores específicos:
# SERVER_IP=localhost                    # Para desarrollo local
# SERVER_IP=192.168.1.100               # Para red local específica  
# SERVER_IP=tudominio.duckdns.org       # Para dominio
# FRONTEND_PORT=9050                     # Puerto del frontend (por defecto: 9050)
# FRONTEND_URL=http://localhost:9050     # Solo si quieres deshabilitar auto-detección

# ===== BASE DE DATOS =====
POSTGRES_USER=foro_user
POSTGRES_PASSWORD=CAMBIAR_ESTE_PASSWORD_AQUI
POSTGRES_DB=forumDB

# ===== SEGURIDAD =====
# 🔐 Generar con: openssl rand -hex 32
JWT_SECRET=generar_nuevo_secret_con_openssl_rand_hex_32
JWT_EXPIRES_IN=7d
EMAIL_VERIFICATION_SECRET=generar_otro_secret_diferente
BCRYPT_ROUNDS=12

# ===== CONFIGURACIÓN DEL SERVIDOR =====
PORT=3000
NODE_ENV=production

# ===== EMAIL CONFIGURATION =====
MAILER_SERVICE=gmail
MAILER_EMAIL=tu-email@gmail.com
MAILER_SECRET_KEY=tu_app_password_gmail

# 📧 Para configurar Gmail:
# 1. Activar autenticación de 2 factores
# 2. Ir a: Cuenta Google → Seguridad → Autenticación en dos pasos → Contraseñas de aplicaciones
# 3. Crear contraseña para "Aplicación personalizada" 
# 4. Usar esa contraseña de 16 caracteres en MAILER_SECRET_KEY

# ===== CONFIGURACIÓN AVANZADA =====
ENABLE_QUERY_LOGGING=false
CONNECTION_POOL_SIZE=20

# ===== EJEMPLOS DE CONFIGURACIÓN =====

# 📍 DESARROLLO LOCAL:
# (No necesitas configurar nada, se auto-detecta)
# Resultado: http://localhost:9050

# 📍 SERVIDOR DE CASA:
# SERVER_IP=192.168.1.100
# FRONTEND_PORT=9050
# Resultado: http://192.168.1.100:9050

# 📍 VPS CON DOMINIO:
# SERVER_IP=midominio.duckdns.org  
# Resultado: https://midominio.duckdns.org

# 📍 VPS CON IP PÚBLICA:
# SERVER_IP=203.0.113.1
# FRONTEND_PORT=8080
# Resultado: http://203.0.113.1:8080

# ===== NOTAS IMPORTANTES =====
# ✅ FRONTEND_URL se genera automáticamente según SERVER_IP y FRONTEND_PORT
# ✅ PgBouncer está configurado con fallback automático a PostgreSQL directo
# ✅ Si no configuras SERVER_IP, detecta automáticamente tu IP local
# ✅ Los dominios (.com, .org, etc.) usan HTTPS automáticamente
# ✅ Las IPs usan HTTP con puerto automáticamente
# ⚙️ Solo configura manualmente si la auto-detección no funciona para tu caso